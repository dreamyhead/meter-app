export interface Records {
  id: number,
  meterId: number,
  date: string,
  lastDateTime: Date,
  dateTime: Date,
  activeReception: number,
  activeReturn: number,
  reactiveReception: number,
  reactiveReturn: number
}
